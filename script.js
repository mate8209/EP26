// Configuración de Reveal.js manteniendo la estructura exacta
Reveal.initialize({
    controls: true,
    progress: true,
    hash: true,
    history: true,
    center: true,
    transition: 'slide', // Mantiene la transición de PowerPoint fluida
    transitionSpeed: 'default'
});
